#include <stdio.h>

int main() {
    int A, B;
    
    // Read two integers
    scanf("%d %d", &A, &B);
    
    // Compare and print the greater number
    if (A > B)
        printf("%d", A);
    else
        printf("%d", B);
        
    return 0;
}